const { expect } = require('chai')
const createCategory = require('../create-category')

describe('create-category', () => {
    it('should respond with 400 if the request body validation fails', () => {
        
    })

    it('should respond with 409 if there is a name conflict', () => {

    })

    it('should pass', () => {

    })

    it('should respond with 500 if saving a category fails', () => {

    })
})